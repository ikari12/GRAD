# Workstation lab bucket (not a service bucket): gs://grad-lab
# Raw FitRec source (too large; download via run_all.sh Step 0)
data/raw/
data/endomondoHR.json

# Python

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
