# Workstation lab bucket (not a service bucket): gs://grad-lab
# Raw FitRec JSON is canonical at gs://yamap-gpx-lab/research/data/endomondoHR.json
# (see lab_assets.json). Do not keep a local copy.
data/raw/
data/endomondoHR.json

# Python

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
