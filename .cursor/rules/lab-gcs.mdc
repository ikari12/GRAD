---
description: Cloud Agent / Cursor Web reads private lab GCS via Cursor OIDC WIF
alwaysApply: true
---

# Lab GCS (Cloud Agent / Cursor Web)

Large artifacts live in this repo's private `*-lab` bucket, not Git. `.cursor/environment.json` `start` runs `python3 lab_gcs.py boot`: mint Cursor OIDC, federate to `cursor-lab-reader`, pull `lab_assets.json` (skip FitRec-scale dumps). Do not ask for JSON keys. Do not use production buckets.

Verified WIF subject: `user:383361105` (`hisashi@ikari.io`). OIDC: https://cursor.com/docs/cloud-agent/identity

```bash
python3 lab_gcs.py probe
python3 lab_gcs.py boot
```
